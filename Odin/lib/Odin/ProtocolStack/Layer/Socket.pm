package Odin::ProtocolStack::Layer::Socket;

use strict;
use warnings;

=head1 NAME

Odin::ProtocolStack::Layer::Socket

=head1 DESCRIPTION

The socket layer inside the stack is the lowest level.
Handles reading (/ writing) from (/to) a given socket that is set at object
creation time.

=cut




1;
