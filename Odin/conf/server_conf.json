{
    "socket": {
        "listen_queue": 10,
        "reuse_address": 1,
        "port": 443,
        "timeout": 300,
        "blocking": 0,
        "verify_mode": "0x01"
    },

    "server": {
        "pk_file": "server.key",
        "cert_file": "server.crt"
    },

    "CA": {
        "root_dir": "/mn/hgfs",
        "cert_file": "certificate_authority.crt"
    }
}
