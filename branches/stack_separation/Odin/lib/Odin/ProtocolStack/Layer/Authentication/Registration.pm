package Odin::ProtocolStack::Layer::Authentication::Registration;

use strict;
use warnings;

=head1 NAME

Odin::ProtocolStack::Layer::Authentication::Authorization

=head1 DESCRIPTION

Registration is the sub-layer of Authentication through which new clients
can identify themselves and request credentials for future use.

=cut

use base qw( Odin Class::Accessor );


1;
